# 🌤 Погодное приложение на Django

Простое веб-приложение для отображения текущей погоды в выбранных городах с использованием OpenWeatherMap API и фреймворка Django.

---

## 📦 Возможности

- Просмотр погоды в сохранённых городах (из базы данных)
- Поиск погоды по названию любого города мира
- Отображение:
  - Температуры (в °C)
  - Описания погоды
  - Влажности (%)
  - Атмосферного давления (гПа)
  - Скорости ветра (м/с)
  - Иконки текущих погодных условий
- Поддержка русского языка
- Адаптивный интерфейс (Bootstrap 5)

---

## 🛠 Технологии

- **Backend**: Python 3.10+, Django 4.x
- **Frontend**: HTML5, Bootstrap 5, Bootstrap Icons
- **API**: [OpenWeatherMap Current Weather API](https://openweathermap.org/current)
- **Зависимости**: `requests`, `python-dotenv`

---

## 🚀 Установка и запуск

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/ваш-логин/weather-app.git
cd weather-app
```

### 2. Создайте виртуальное окружение

```bash
python -m venv venv
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate     # Windows
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

Пример `requirements.txt`:
```txt
Django>=4.2
requests
python-dotenv
```

### 4. Настройте переменные окружения

Создайте файл `.env` в корне проекта:

```env
WEATHER_API_KEY=ваш_ключ_openweathermap
```

> 💡 **Как получить API-ключ**:
> 1. Зарегистрируйтесь на [OpenWeatherMap](https://home.openweathermap.org/users/sign_up)
> 2. Подтвердите email
> 3. Перейдите в раздел **[API keys](https://home.openweathermap.org/api_keys)**
> 4. **Обязательно активируйте бесплатную подписку**:
>    - Зайдите в **Billing plans**
>    - Нажмите **Subscribe** на тариф **"Current Weather and Forecasts Collection"**
>    - Без этого шага будет ошибка `401 Invalid API key`

### 5. Выполните миграции

```bash
python manage.py makemigrations
python manage.py migrate
```

### 6. (Опционально) Добавьте города в базу

Вы можете добавить города через Django Admin или создать fixture.

Пример модели `City` (`models.py`):

```python
from django.db import models

class City(models.Model):
    name = models.CharField(max_length=100, unique=True)

    def __str__(self):
        return self.name
```

### 7. Запустите сервер

```bash
python manage.py runserver
```

Откройте в браузере: [http://127.0.0.1:8000](http://127.0.0.1:8000)

---

## 📝 Использование

1. Введите название города в поле формы и нажмите **«Получить погоду»**
2. Результат отобразится в правой части страницы
3. Сохранённые города (из модели `City`) показываются автоматически

---

## ⚠️ Важно

- Бесплатный план OpenWeatherMap позволяет делать **до 1000 запросов в день** и **60 запросов в минуту**
- Приложение делает отдельный запрос к API **для каждого города** при каждом обновлении страницы
- Для продакшена рекомендуется:
  - Добавить кэширование (`django.core.cache`)
  - Ограничить число отображаемых городов
  - Использовать Celery для фонового обновления погоды

---

## 👤 Автор

**Ivan Romanov**

---
*Создано с ❤️ для обучения и развития*

---

## 📄 Лицензия

Данные погоды предоставляются через [OpenWeatherMap](https://openweathermap.org/).  
В соответствии с условиями бесплатного тарифа, **обязательно указывайте атрибуцию**:

> **Weather data provided by [OpenWeather](https://openweathermap.org/)**

---

*Удачного прогноза! ☀️🌧️❄️*